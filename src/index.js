import React from 'react';
// react dom
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

// React is imported to write jsx.
// In react we write code in jsx.
// Jsx stands for Javascript xml.(xml => extensible markup language => treat as HTML.)
// Jsx = Javascript + HTMl(jsx is a combination language of javascript and HTML)

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render( <
    App / >

);

reportWebVitals();

// component => meaning part of readable code.